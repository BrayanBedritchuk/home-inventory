<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.sailboat.homeinventory">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".view.LaunchActivity">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <activity android:name=".view.dashboard.DashboardActivity" />

        <activity android:name=".view.shop.ShopActivity" />

        <activity android:name=".view.product.list.ProductListActivity" />

        <activity android:name=".view.product.details.ProductDetailsActivity" />

        <activity
            android:name=".view.product.insert.ProductInsertActivity"
            android:windowSoftInputMode="stateVisible" />

    </application>

</manifest>
