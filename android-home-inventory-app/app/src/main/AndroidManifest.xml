<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.sailboat.homeinventory">

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".ui.LaunchActivity"
            android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.dashboard.DashboardActivity" />

        <activity android:name=".ui.product.list.ProductListActivity" />

        <activity android:name=".ui.product.details.ProductDetailsActivity" />

        <activity
            android:name=".ui.product.insert.ProductInsertActivity"
            android:windowSoftInputMode="stateVisible" />

        <activity android:name=".ui.shopping.ShoppingActivity" />

    </application>

</manifest>
